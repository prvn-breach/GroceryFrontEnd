version: '3'
services:
  grocery-node:
    container_name: grocery-node
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
       - ../:/var/www
    command: tail -f /dev/null
    ports:
      - "9090:8080"
